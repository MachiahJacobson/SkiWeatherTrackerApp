//
//  ContentView.swift
//  SkiWeatherTrackerApp
//
//  Created by Jacobson, Machiah - Student on 11/18/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
